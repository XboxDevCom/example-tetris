<Page
    x:Class="Tetris.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Tetris"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    UseSystemFocusVisuals="True"
    Background="#1E1E1E"
    KeyDown="Page_KeyDown">

    <Grid Padding="40">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="40" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <Grid Grid.Column="0"
              HorizontalAlignment="Left"
              VerticalAlignment="Center">
            <Canvas x:Name="GameArea"
                    Width="200"
                    Height="400"
                    Background="#0A0A0A"
                    HorizontalAlignment="Left"
                    VerticalAlignment="Top" />
        </Grid>

        <StackPanel Grid.Column="2"
                    Orientation="Vertical"
                    Spacing="16"
                    VerticalAlignment="Top">
            <TextBlock Text="Tetris"
                       FontSize="48"
                       FontWeight="Bold"
                       Foreground="White"
                       HorizontalAlignment="Center" />

            <Border Background="#2D2D2D"
                    CornerRadius="8"
                    Padding="16">
                <StackPanel Orientation="Vertical" Spacing="8">
                    <TextBlock Text="Punkte" FontSize="16" Foreground="#AAAAAA" />
                    <TextBlock x:Name="ScoreText" Text="0" FontSize="32" FontWeight="Bold" Foreground="White" />
                </StackPanel>
            </Border>

            <Border Background="#2D2D2D"
                    CornerRadius="8"
                    Padding="16">
                <StackPanel Orientation="Vertical" Spacing="8">
                    <TextBlock Text="Stufe" FontSize="16" Foreground="#AAAAAA" />
                    <TextBlock x:Name="LevelText" Text="1" FontSize="32" FontWeight="Bold" Foreground="White" />
                </StackPanel>
            </Border>

            <Border Background="#2D2D2D"
                    CornerRadius="8"
                    Padding="16">
                <StackPanel Orientation="Vertical" Spacing="8">
                    <TextBlock Text="Zeilen" FontSize="16" Foreground="#AAAAAA" />
                    <TextBlock x:Name="LinesText" Text="0" FontSize="32" FontWeight="Bold" Foreground="White" />
                </StackPanel>
            </Border>

            <Border Background="#2D2D2D"
                    CornerRadius="8"
                    Padding="16">
                <StackPanel Orientation="Vertical" Spacing="8">
                    <TextBlock Text="Nächstes" FontSize="16" Foreground="#AAAAAA" />
                    <Canvas x:Name="NextPieceArea"
                            Width="120"
                            Height="120"
                            HorizontalAlignment="Left" />
                </StackPanel>
            </Border>

            <Button x:Name="StartButton"
                     MinHeight="48"
                    Content="Start"
                    FontSize="20"
                    Padding="24,12"
                    Background="#0078D7"
                    Foreground="White"
                    Click="StartButton_Click"
                    HorizontalAlignment="Stretch" />

            <TextBlock x:Name="GameOverText"
                       Text="Spiel beendet!"
                       FontSize="28"
                       FontWeight="Bold"
                       Foreground="Red"
                       HorizontalAlignment="Center"
                       Visibility="Collapsed" />

            <TextBlock Text="Steuerung: Links/Rechts - Bewegen, Unten - Soft Drop, A - Drehen, Oben - Hard Drop"
                       FontSize="12"
                       Foreground="#888888"
                       TextWrapping="Wrap"
                       HorizontalAlignment="Center" />
        </StackPanel>
    </Grid>
</Page>
